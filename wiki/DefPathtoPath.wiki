#summary Convert SVG paths into transformable Paths
#labels Reference

= pathtoPath =

Converts `SVG("path", d="...")` into `Path(d=[...])`.

== Arguments ==

*path(svg)*

|| svg || _*required*_ || an SVG path ||
